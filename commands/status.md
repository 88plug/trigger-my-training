---
description: Show the trigger-my-training grounding gate state for this session
---

Run `tmt-ground status` and report the result: whether the session has been
grounded (`grounded`) and which read-only probes are on record. If no active
session is found, say the gate is idle (nothing has hit it yet this session).
